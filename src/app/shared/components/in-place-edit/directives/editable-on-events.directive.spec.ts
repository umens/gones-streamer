import { EditableOnEventsDirective } from './editable-on-events.directive';

describe('EditableOnEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableOnEventsDirective();
    expect(directive).toBeTruthy();
  });
});
