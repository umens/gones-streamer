import { SceneItemTransform } from './scene-item-transform.model';

describe('SceneItemTransform', () => {
  it('should create an instance', () => {
    expect(new SceneItemTransform()).toBeTruthy();
  });
});
