import { GradiantDirective } from './gradiant.directive';

describe('GradiantDirective', () => {
  it('should create an instance', () => {
    const directive = new GradiantDirective();
    expect(directive).toBeTruthy();
  });
});
