import { AdapteTextColorToBgDirective } from './adapte-text-color-to-bg.directive';

describe('AdapteTextColorToBgDirective', () => {
  it('should create an instance', () => {
    const directive = new AdapteTextColorToBgDirective();
    expect(directive).toBeTruthy();
  });
});
